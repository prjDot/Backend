services:
  app:
    image: ${APP_IMAGE:-docker.io/myagmb28/pawgun:latest}
    container_name: pogun-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: ${PORT:-8080}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JAVA_STARTUP_OPTS: ${JAVA_STARTUP_OPTS:--XX:+UseG1GC -XX:MaxGCPauseMillis=100 -XX:+AlwaysPreTouch -XX:+UseStringDeduplication -XX:+TieredCompilation -XX:TieredStopAtLevel=1 -Djava.security.egd=file:/dev/./urandom}
      APP_STARTUP_WARMUP_ENABLED: ${APP_STARTUP_WARMUP_ENABLED:-true}
      APP_SCHEMA_SYNC_ENABLED: ${APP_SCHEMA_SYNC_ENABLED:-true}
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "${PORT:-8080}:8080"
    healthcheck:
      test: ["CMD", "/usr/local/bin/healthcheck-adaptive.sh"]
      interval: 2s
      timeout: 5s
      retries: 12
      start_period: 90s

  redis:
    image: redis:7-alpine
    container_name: pogun-redis
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    command: >
      sh -c '
        if [ -n "$$REDIS_PASSWORD" ]; then
          exec redis-server --appendonly yes --requirepass "$$REDIS_PASSWORD";
        else
          exec redis-server --appendonly yes;
        fi
      '
    volumes:
      - redis-data:/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "if [ -n \"$$REDIS_PASSWORD\" ]; then redis-cli -a \"$$REDIS_PASSWORD\" ping; else redis-cli ping; fi",
        ]
      interval: 10s
      timeout: 3s
      retries: 5

volumes:
  redis-data:
